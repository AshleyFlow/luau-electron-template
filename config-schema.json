{
    "properties": {
        "lunePort": {
            "type": "integer",
            "description": "The port that lune is going to listen on"
        },
        "mainScript": {
            "type": "string",
            "description": "Path must be relative to root directory"
        },
        "cwd": {
            "type": "string",
            "description": "Don't change this, it will automatically get assigned to during build"
        },
        "executable": {
            "type": "string",
            "description": "Don't change this, it will automatically get assigned to during build"
        }
    }
}