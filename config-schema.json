{
    "properties": {
        "lunePort": {
            "type": "integer",
            "description": "The port that lune is going to listen on"
        },
        "mainScript": {
            "type": "string",
            "description": "Path must be relative to root directory"
        },
        "executable": {
            "type": "string",
            "description": "Path must be relative to root directory\nThis path automatically gets set during packaging, so you don't need to worry about it"
        }
    }
}